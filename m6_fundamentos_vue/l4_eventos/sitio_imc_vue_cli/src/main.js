import { createApp } from 'vue' // Importa la función createApp desde el paquete 'vue' para crear la aplicación Vue
import App from './App.vue' // Importa el componente raíz de la aplicación desde el archivo App.vue

createApp(App).mount('#app') // Crea la aplicación Vue utilizando el componente App y la monta en el elemento del DOM con el id 'app'
