npm init -y  // Inicializar proyecto
npm install webpack webpack-cli style-loader css-loader --save-dev // Instalar dependencias webpack
npm run build // Construir proyecto (se usa tmb para actualizaciones del proyecto)
